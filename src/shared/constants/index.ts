export const enum Cookies {
  ROLE = 'role',
}

export const enum ROLE {
  ADMIN = 'admin',
  USER = 'user',
}

export const PRIVATE_PATHS = ['/', '/logout'];

export const NO_AUTH_PATHS = ['/login'];
